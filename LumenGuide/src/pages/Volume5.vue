<template>
  <VolumeShell>
    <section class="chapter" id="ch5-1">
      <h2 class="chapter-title">第1章 · 账号与密码</h2>
      <div class="chapter-body">
        <p>（这是《第五卷 · 安全与防护 · 第1章》的正文占位。在 <code>src/pages/Volume5.vue</code> 里找到 <code>id="ch5-1"</code> 的 <code>&lt;section&gt;</code>，把内容写进它的 <code>.chapter-body</code> 即可。）</p>
      </div>
    </section>
    <section class="chapter" id="ch5-2">
      <h2 class="chapter-title">第2章 · 病毒与防护</h2>
      <div class="chapter-body">
        <p>（这是《第五卷 · 安全与防护 · 第2章》的正文占位。在 <code>src/pages/Volume5.vue</code> 里找到 <code>id="ch5-2"</code> 的 <code>&lt;section&gt;</code>，把内容写进它的 <code>.chapter-body</code> 即可。）</p>
      </div>
    </section>
    <section class="chapter" id="ch5-3">
      <h2 class="chapter-title">第3章 · 隐私设置</h2>
      <div class="chapter-body">
        <p>（这是《第五卷 · 安全与防护 · 第3章》的正文占位。在 <code>src/pages/Volume5.vue</code> 里找到 <code>id="ch5-3"</code> 的 <code>&lt;section&gt;</code>，把内容写进它的 <code>.chapter-body</code> 即可。）</p>
      </div>
    </section>
  </VolumeShell>
</template>

<script setup lang="ts">
import VolumeShell from './VolumeShell.vue';
</script>
