<template>
  <VolumeShell>
    <section class="chapter" id="ch9-1">
      <h2 class="chapter-title">第1章 · 故障自救思路</h2>
      <div class="chapter-body">
        <p>（这是《第九卷 · 故障排除思维 · 第1章》的正文占位。在 <code>src/pages/Volume9.vue</code> 里找到 <code>id="ch9-1"</code> 的 <code>&lt;section&gt;</code>，把内容写进它的 <code>.chapter-body</code> 即可。）</p>
      </div>
    </section>
    <section class="chapter" id="ch9-2">
      <h2 class="chapter-title">第2章 · 常见蓝屏处理</h2>
      <div class="chapter-body">
        <p>（这是《第九卷 · 故障排除思维 · 第2章》的正文占位。在 <code>src/pages/Volume9.vue</code> 里找到 <code>id="ch9-2"</code> 的 <code>&lt;section&gt;</code>，把内容写进它的 <code>.chapter-body</code> 即可。）</p>
      </div>
    </section>
    <section class="chapter" id="ch9-3">
      <h2 class="chapter-title">第3章 · 求助与排查</h2>
      <div class="chapter-body">
        <p>（这是《第九卷 · 故障排除思维 · 第3章》的正文占位。在 <code>src/pages/Volume9.vue</code> 里找到 <code>id="ch9-3"</code> 的 <code>&lt;section&gt;</code>，把内容写进它的 <code>.chapter-body</code> 即可。）</p>
      </div>
    </section>
  </VolumeShell>
</template>

<script setup lang="ts">
import VolumeShell from './VolumeShell.vue';
</script>
